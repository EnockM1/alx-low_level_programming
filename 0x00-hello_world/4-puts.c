#include <stdio.h>
#include <string.h>

/**
* main-Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	char str1[40];
	strcpy(str, "Programming is like building a multilingual puzzle");
	puts(str1);
	return (0);
}
